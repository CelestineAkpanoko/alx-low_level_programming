#include <stdio.h>

/**
 * main - Prints all single digit number of base 10.
 *
 * Return: Always 0.
 */
int main(void)
{
int i;
for (i = 0; i < 10; i++)
	printf("%d", i);
	if (i == 9)
		printf("\n");
return (0);
}
